<!DOCTYPE html>
<html class="h-full" data-controller="theme">
  <head>
    <title><%= content_for?(:title) ? yield(:title) + " | " : "" %>Read Junior Classics</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'favicon-blue.ico' %>
 
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script defer src="https://cloud.umami.is/script.js" data-website-id="f43e0ee2-b683-4fa6-99bc-12fb34a31c88"></script>
  </head>

  <body class="flex flex-col min-h-screen bg-gray-50 dark:bg-slate-900">
    <%= render 'shared/header' %>

    <main class="flex-grow">
      <div class="container mx-auto px-4 py-8">
        <%= render 'shared/flash_messages' %>
        <%= render 'shared/breadcrumbs' %>
        <%= yield %>
      </div>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
