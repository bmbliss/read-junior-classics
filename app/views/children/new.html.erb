<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">Add New Child</h1>
  
  <%= render 'form', user: @user, child: @child %>
  
  <%= link_to 'Back to Children', user_children_path(@user), class: 'text-primary-500 hover:text-primary-600' %>
</div>
