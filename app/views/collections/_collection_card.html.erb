<div class="bg-white dark:bg-slate-800 shadow-md rounded-lg p-6">
  <h3 class="text-xl font-semibold mb-2"><%= link_to collection.name, collection_path(collection), class: "text-blue-600 hover:text-blue-800" %></h3>
  <p class="text-gray-600 dark:text-slate-400 mb-4"><%= truncate(collection.description, length: 100) %></p>
  <p class="text-sm text-gray-500">
    <%= pluralize(collection.literary_works.count, 'work') %>
  </p>
</div>
